#!/bin/bash

for file in "$@"; do
    name=${file%.*}
    if [ ! -f ~/scratch/orcacalc/$name/$name.out ]; then
        continue
    fi
    echo "Output file: ~/scratch/orcacalc/$name/$name.out"
    tail -n 15 ~/scratch/orcacalc/$name/$name.out
done 